#include <iostream>
#include "Rectangle.h"
using namespace std;

bool Rectangle::InitMembers(const Point& ui, const Point& ir)
{
	if (ui.GetX() > ir.GetX() || ui.GetY() > ir.GetY())
	{

	}
}